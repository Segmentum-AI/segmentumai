export default function Problem() {
  return (
    <section className="py-24">
      <h2 className="text-3xl font-semibold">The problem</h2>
      <p className="mt-4 text-muted-foreground max-w-2xl">
        Biomedical segmentation is slow, data-hungry, and brittle. Existing
        pipelines require thousands of annotations and fail to generalize across
        modalities, institutions, and rare structures.
      </p>
    </section>
  )
}
