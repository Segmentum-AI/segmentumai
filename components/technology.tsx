export default function Technology() {
  return (
    
    <section className="py-24">
      <h2 className="text-3xl font-semibold">
        Few-shot segmentation, redefined
      </h2>
      <p className="mt-4 text-muted-foreground max-w-2xl">
        Segmentum AI adapts to new biomedical tasks with as few as 1–5 annotations.
        Our models generalize across domains without full retraining, enabling
        production-ready segmentation in minutes.
      </p>
    </section>
  )
}
